# secbrain — Second Brain Vault

Markdown + Git + Claude. 이 저장소의 상태 전부가 `.md` 파일입니다. 데이터베이스도, 서버도 없습니다.

## 폴더 규약

| 폴더 | 용도 | 언제 쓰나 |
|---|---|---|
| `00-inbox/` | 미분류 캡처 | 분류가 애매하면 일단 여기 |
| `01-projects/` | 끝이 있는 일 | 마감·목표가 있는 것 |
| `02-areas/` | 계속 유지하는 영역 | 건강, 커리어, 팀 운영 |
| `03-resources/` | 참고 자료 | 링크, 발췌, 레퍼런스 |
| `04-archive/` | 종료·보관 | 끝났지만 지우긴 아까운 것 |
| `05-daily/` | 일자별 기록 | 데일리 브리프, 그날의 캡처 |
| `06-templates/` | 노트 템플릿 | 새 노트의 출발점 |

## 노트 형식

모든 노트는 프론트매터로 시작합니다.

```markdown
---
title: 노트 제목
date: 2026-07-30
domain: projects        # inbox | projects | areas | resources | archive | daily
tags: [ai, agent]
summary: 한 줄 요약. 사이트 카드에 그대로 노출됩니다.
---

본문. [[다른-노트]] 로 연결하고 #태그 를 씁니다.

- [ ] 할 일은 Obsidian Tasks 형식으로 📅 2026-08-05
```

규칙:

- **파일명은 kebab-case**, 확장자 `.md`. 파일명이 곧 `[[wikilink]]` 대상입니다.
- `summary`는 반드시 채웁니다. 사이트의 카드 미리보기가 이 값을 씁니다.
- 날짜는 항상 `YYYY-MM-DD` 절대 표기. "지난주" 같은 상대 표현을 노트에 남기지 않습니다.
- 태스크는 `- [ ] 내용 📅 YYYY-MM-DD` — 이 형식이어야 사이트 대시보드가 집계합니다.
- 링크는 `[[파일명]]` (확장자 없이). 아직 없는 노트를 가리켜도 괜찮습니다 — 쓸 거리 표시입니다.

## 스킬

`.claude/skills/` 아래가 이 브레인의 동작 전부입니다. 코드가 아니라 마크다운 지시문입니다.

- `braindump` — 뒤엉킨 생각 덩어리를 도메인별로 쪼개 각 폴더에 배치
- `daily-brief` — 관심사 기반 뉴스 브리프, 출처 필수
- `weekly-review` — 한 주 캡처를 교차 분석해 패턴 추출
- `consolidate` — 흩어진 노트를 재사용 가능한 프레임워크로 승격

## 절대 하지 말 것

- 노트를 **삭제**하지 마세요. 끝난 것은 `04-archive/`로 옮깁니다.
- `site/`, `scripts/`, `.github/` 는 프레임워크입니다. 브레인덤프가 여기에 쓰지 않습니다.
- 프론트매터 없는 `.md`를 만들지 마세요. 빌드에서 누락됩니다.
- 출처 없는 주장을 브리프에 넣지 마세요. 못 찾으면 "확인 불가"로 남깁니다.

## 빌드

`node scripts/build.mjs` → `dist/` 에 정적 사이트 생성. GitHub Actions가 push마다 실행합니다.
